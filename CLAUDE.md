# منصّة حجز — EMAN AAL MOUSA

دليل التشغيل لمن يفتح هذا المستودع أوّل مرّة. اقرأه كاملًا قبل أيّ تعديل:
فيه قراراتٌ اتُّخذت عن قصد، ونقضُها بلا سببٍ جديد يُعيد أعطابًا سبق أن
وقعت.

**اللغة:** كلّ شيء بالعربية — الواجهتان والشروح ورسائل الكومت. أكمل بها.

---

## ١) ما هذا

منصّة حجز مواعيد لميك اب أرتست في الرياض. صفحتان اثنتان لا أكثر:

| | الملفّ | من يفتحه |
|---|---|---|
| صفحة العميلة | `index.html` | من تحجز موعدًا |
| لوحة التحكّم | `admin.html` | صاحبة العمل وحدها |

بلا إطارٍ برمجيّ ولا خطوة بناء: HTML و CSS و JavaScript صِرفة (وحدات
ES). القاعدة Supabase، والنشر GitHub Pages من جذر الفرع مباشرة.

- الحيّ: <https://eman-aalmousa.com> · `/admin.html`
- المختبر: <https://eman-aalmousa.com/lab/> · `/lab/admin.html`

---

## ٢) القاعدة الذهبية: المختبر أوّلًا

الجذر هو **ما يراه الناس الآن**. `lab/` هو **مكان العمل**.

```
كلّ تعديل يهبط في lab/ أوّلًا.  الجذر لا يُلمس إلّا بالترقية.
```

الملفّان متطابقان **بايتًا ببايت**؛ الصفحة تعرف موضعها من مسارها فتتكلّم
في `/lab/` وتصمت في الجذر: شارةٌ حمراء، وبلا service worker، و `noindex`.
فالترقية نسخٌ محض لا تحويل، ولا يبقى فرقٌ صامت بين ما جُرّب وما نُشر.

```bash
bash tools/promote.sh --check    # هل الجذر يطابق المختبر؟
bash tools/promote.sh            # ارفع المختبر إلى الحيّ ثم تحقّق
```

### ما لا يعزله المختبر — وهذا أوقعنا مرّة

**القاعدة ودوالّ الحافة (Edge Functions) مشتركة بين النسختين.** ما يُنشر
منهما يصل الحيَّ في اللحظة نفسها. المختبر يعزل **الصفحات وحدها**.

> وقع فعلًا: نُشرت دالّة قراءة الإيصال وقد غُيّر شكل ردّها، والصفحة
> الحيّة كانت تحكم من ذلك الردّ. فرفضت إيصالاتٍ سليمة حتى رُقّيت.

**القاعدة:** أيّ تغيير في القاعدة أو في دالّة حافة إمّا أن يبقى متوافقًا
مع الصفحة القديمة، وإمّا أن تُرقّى الصفحات معه في الدفعة نفسها.

---

## ٣) الفحص

٣٥ طقمًا، تقيس **ما يُرى في المتصفّح** لا ما كُتب في المصدر: أحجام
الخطوط، ونسب التباين من البكسل، وهندسة العناصر، وحمولة الحفظ.

```bash
python3 tests/sync.py        # يبني build/testroot من lab/  — قبل كل تشغيل
python3 tests/run.py         # الطُّقُم كلّها
python3 tests/run.py check_month check_receipt2
python3 tests/run.py --list
```

يلزمه Python 3.11+ و Playwright:

```bash
pip install playwright && python3 -m playwright install chromium
```

لا تحتاج الطُّقُم قاعدةً حيّة: `dev/mock-supabase.js` يقوم مقامها ويُحقن
باعتراض `assets/vendor/supabase.js`. ومنطق القاعدة يُفحص بملفّات SQL في
`supabase/tests/` داخل معاملةٍ تُرجَع بلا أثر.

| الطقم | ما يحرسه |
|---|---|
| `check_fit` | مقاسات وهندسة صفحة العميلة (١٢٢) |
| `check_group` | الحجز الجماعي (٥٠) |
| `check_month` | لوح الشهور والشهر المغلق (٤٢) |
| `check_receipt2` | حارس الإيصال وصمت الرفض (٢٨) |
| `check_backup` | النسخة الاحتياطية: ما تحمله وما تستعيده (٢٠) |
| `check_auto` | الرسائل التلقائية (٢٥) |
| `check_visits` | عدّ الزوّار وحمولته وحصائله (٢٥) |
| `check_media` | وسيطة الخدمة كما تُرسم في البطاقة والسلّة (١٠) |
| `check_sw` | وصولُ النسخة الجديدة من أوّل زيارة، والعمل بلا شبكة (٦) |
| `check_lab` | فصل المختبر عن الحيّ (١٨) |
| `check_assets` | بصمة الأصول `?v=` |

---

## ٤) أعطابٌ وقعت — لا تُعِدها

| العطب | الحارس اليوم |
|---|---|
| تغيّر أصلٌ وبقي `?v=` فخدم المتصفّح نسخته المحفوظة | `check_assets` — ارفع الرقم في `lab/index.html` و`lab/admin.html` مع كلّ تغيير في `assets/` |
| صنفُ CSS مشترك ابتلع لون نصّ (`.wa`) فاختفى | `check_receipt2` يقيس نسبة التباين من البكسل |
| ملفٌّ بلا `<meta charset>` يُفتح من القرص فتنقلب العربية | `make_standalone` يلفّه بمستندٍ كامل |
| `lpad` يقصّ حين يتجاوز الرقمُ العرض | `testedge` |
| محاكي التطوير تخلّف عن المخطّط فعطّل الفحص صامتًا | ثبّت الأساس على HEAD قبل اتّهام المنتَج |
| القراءة تقبل أيّ صورة فيها رقمٌ كبير | الآيبان صار شرطًا — §٦ |
| خمسة طُقُم لا تُخرج رمز فشل، فتمرّ في المُشغِّل وهي ساقطة | كلُّها تُنهي بـ `sys.exit` الآن؛ ولا يُقبل طقمٌ جديد بلا ذلك |
| `<i>` سطريّ لا ارتفاع له فاختفى شريط النسبة | `check_visits` يقيس ارتفاع الشريط وعرضه ولونه |
| «إخفاء» يُخرج الخدمة من اللوحة أيضًا، فلا تُستعاد ولا تُرى وسيطتها | قسم «خدمات مخفية» في اللوحة ومعه زرّ «إظهار» |
| فيديو بلا ملصق يُخرج `url('')` فتظهر البطاقة صمّاء | `check_media` يرفض أيّ خلفيّة فارغة، واللوحة تلتقط ملصقًا من الفيديو |
| احتياطٌ يُختار بترتيب الخدمة (`UI.media[i]`) فتلبس خدمةٌ وجهَ غيرها | الاحتياط بالاسم وحده، و`check_media` يمنع تسرّب وجهٍ آخر |
| `eq()` في محاكي التطوير لا يرشّح، فترى الطُّقُم صفوفًا لا تصل المتصفّح | `eq` يرشّح الآن؛ ومحاكٍ يكذب يُعطّل التغطية بصمت |
| عاملُ الخدمة يخدم المستند المحفوظ أوّلًا، فأوّل زيارة بعد كلّ نشرٍ ترى النسخة السابقة | المستند من الشبكة أوّلًا و`check_sw` يقيسه؛ الأصول تبقى من المحفوظ لأنّ `?v=` يكفيها |
| اللقطة على ستّة جداول والمخطّط ثلاثة عشر: الرسائل والطابور والعدّاد والزوّار والسجلّ خارجها | `snapshot_coverage.sql` يقارن مفاتيح اللقطة بجداول المخطّط، و`check_backup` يفحص الملفّ المنزَّل |
| الاستعادة تسقط كلُّها على مشغّل «رابط الخرائط مطلوب» — نسخةٌ تُؤخذ ولا تُستعاد | رايةُ `app.restoring` داخل المعاملة — §١١ |
| الاستعادة تجدول رسائل واتساب لحجوزاتٍ انتهت من شهور | الرايةُ نفسها توقف `tg_schedule_auto_messages` |
| استعادة الإعدادات بقائمة أعمدةٍ مكتوبة باليد تُسقط اثني عشر إعدادًا صامتةً | الدمج بكلّ الأعمدة من `information_schema` |
| التنزيل واللقطة السحابية يقرآن جدولين مختلفين، و«نسخة كاملة» بمعنيين | `exportAll` صار ينادي `admin_snapshot` — مصدرٌ واحد |

---

## ٥) القاعدة

مشروع Supabase واحد. الترحيلات في `supabase/migrations/` بالترتيب.

**قواعد لا تُخالَف:**

- **لا تُعدَّل بياناتٌ حيّة بلا إذنٍ صريح من صاحب المشروع.**
- أيّ تغيير في المخطّط يُجرَّب أوّلًا داخل كتلةٍ ترفع استثناءً في آخرها
  فتُرجَع المعاملة كاملة:
  ```sql
  do $do$ begin
    -- التغيير والفحص هنا
    raise exception 'ROLLBACK_OK %', النتيجة;
  end $do$;
  ```
  ثم يُتحقَّق من صفر أثر، ثم يُطبَّق.
- الأسرار في خزنة Supabase (Vault) أو أسرار الدوالّ — **لا في المستودع
  ولا في عمودٍ يُقرأ بـ `select`**. مفتاح القراءة `OCR_API_KEY` ومفتاح
  واتساب `WA_TOKEN` كلاهما هناك.
- الحكم في القاعدة لا في المتصفّح. المتصفّح يسأل ويعرض.

الجداول: `bookings` · `booking_items` · `services` · `settings` ·
`availability_rules` · `date_overrides` · `message_templates` ·
`message_outbox` · `receipt_scans` · `activity_log` · `admins` ·
`ref_counters` · `visits`.

---

## ٦) حارس الإيصال

العميلة تحوّل العربون وترفع صورة الإشعار. الحكم في `receipt_state`،
وشرطاه يجتمعان:

1. **آيبان صاحبة العمل** في الصورة — آخر `receipt_iban_digits` خانة
   (ستّ افتراضًا). وهو الدليل الذي لا تحمله صورةٌ من مكانٍ آخر.
2. **مبلغٌ يساوي العربون أو يزيد** — من المبالغ الموسومة بعملةٍ وحدها،
   لا كلِّ رقمٍ في الصورة.

ثلاثة أحوال: `ok` و `bad` و `wait`. و`wait` ليس رفضًا — تعذّر القراءة لا
يُتّهم به صاحب الإيصال.

**ولا يُقال أيُّ الشرطين سقط.** من عرف أنّ المبلغ هو المطلوب زوّر مبلغًا،
ومن عرف أنّ الآيبان مطلوب ألصقه. الجواب واحد: «الإيصال غير صحيح». وحتى
دالّة القراءة لا تردّ إلى المتصفّح أرقامًا ولا كلمات — حرفٌ واحد.

> **لا تُضِف سببًا إلى رسالة الرفض ولا تُرجِع تفاصيل الفحص إلى المتصفّح.**
> هذا قرارُ أمنٍ لا نقصٌ في الرسالة.

---

## ٧) الرسائل التلقائية

الرسائل في «الإعدادات ← الرسائل» تُكتب مرّة، وتُرسَل باليد من بطاقة
العميلة أو تخرج وحدها. المُطلِقات: عند وصول الطلب · عند التأكيد · قبل
الموعد بمدّة · بعد اكتمال الخدمة بمدّة.

- الجدولة في `schedule_auto_messages`، والطابور `message_outbox`،
  والإرسال `dispatch_due_messages` ثم `reconcile_sent_messages`،
  و`pg_cron` كلّ خمس دقائق (`tick_messages`).
- **الحال اليوم: وضع المعاينة.** لم يُربط حساب واتساب بعد، فتُختم
  الرسائل `preview` ولا تخرج إلى أحد.
- للتفعيل: `settings.wa_phone_id` + سرّ `WA_TOKEN` في الخزنة.
- رسائل الأعمال عند ميتا لا تُرسَل نصًّا حرًّا: قوالبُها تُعتمد مسبقًا،
  ومتغيّراتها مرقّمة. `wa_template_text` تولّد النصّ كما يُقدَّم لميتا.
  **الترتيب مصدره واحد** — دالّة المتصفّح تُقارَن بدالّة القاعدة في
  `check_wa`؛ لو تباعدتا سقط الفحص.

---

## ٧ب) عدّ الزوّار

عدٌّ بلا تعرّف: معرّفٌ عشوائيّ يصنعه متصفّح الزائرة ويحفظه عندها. **لا
اسم، ولا عنوان شبكة، ولا سلسلة متصفّح، ولا موقع.** إن مسحته صارت زائرةً
جديدة — المطلوب مقياسٌ لا هويّة.

- الجدول `visits`، ونداءاته `track_visit` و`track_ping` و`track_booked`.
- **لا سياسةَ قراءةٍ لأحد على الجدول**: الحصائل تُقرأ بدالّةٍ مالكة
  (`admin_visits` و`admin_visits_monthly`)، ولا يُعرض سطرُ زائرةٍ بعينها.
- «زائر» متصفّحٌ مختلف لا فتحةُ صفحة. و«دخل وخرج» من لم يتجاوز الشاشة
  الأولى. والبقاء من الجلسات التي نبضت فعلًا.
- **المختبر لا يُحسب**، ولا المتصفّح الآليّ، ولا صفحة متابعة الحجز.
- سقفٌ ٤٠ جلسة للمتصفّح في اليوم يمنع نفخ العدّ. وما مضى عليه أكثر من
  ٤٠٠ يوم يُمسح أسبوعيًّا.

> **لا تُضِف حقلًا يدلّ على شخص، ولا تعرض سطرًا مفردًا في اللوحة.** هذا
> قرارُ خصوصيّة لا نقصٌ في الميزة. وقد حُذفت بطاقةُ الشرح من الشاشة بطلب
> صاحب المشروع — والحذف في النصّ وحده: القرار قائم في الجدول والدوالّ.

---

## ٧ج) النسخة الاحتياطية

اللقطة `admin_snapshot` هي **التعريف الوحيد** لكلمة «نسخة كاملة»: منها
تُبنى النسخة السحابية والملفّ المنزَّل معًا. تحمل كلَّ جدولٍ في المخطّط
إلّا اثنين، وكلاهما عن قصد:

- **`admins`** — جدولُ صلاحية لا بيانات. و`user_id` فيه يخصّ مشروعًا
  بعينه فلا معنى لاستعادته في غيره، ونسخةٌ تُسرَّب فتُعيد منح الوصول
  خطرٌ بلا مقابل.
- **`raw_text`** من `receipt_scans` — النصّ الخام لإيصال العميلة البنكيّ.
  مشتقٌّ من الصورة (والصور تُنسخ مع اللقطة)، فحملُه في ملفٍّ يُنزَّل
  توسيعٌ للانكشاف بلا فائدة استعادة. وحكمُ الفحص يبقى كاملًا.

> **جدولٌ جديد يدخل اللقطة أو يُذكر في قائمة الاستثناء.** يحرسه
> `supabase/tests/snapshot_coverage.sql`: يقارن مفاتيح اللقطة بجداول
> المخطّط، فالمنسيّ يُسقط الفحص بدل أن يمرّ صامتًا.

**الاستعادة** تُعيد الغائب ولا تمسّ القائم (`on conflict do nothing`)،
واستثناؤها الوحيد عدّادُ أرقام الحجوزات: يأخذ الأكبر فلا يرجع إلى الوراء
فيتكرّر رقم.

**رايةُ `app.restoring`** ترتفع داخل معاملة الاستعادة وحدها
(`set_config(..., true)`، فتسقط مع نهايتها). تقرؤها `public.restoring()`،
ويحترمها مشغّلان — وهما قواعدُ استقبالٍ لا سلامةِ بيانات:

- `bookings_require_map` — رابط الخرائط شرطُ حجزٍ جديد لا شرطُ صفٍّ
  يعود. بدونها كانت الاستعادة تسقط كلُّها على أوّل حجزٍ قديم بلا رابط.
- `tg_schedule_auto_messages` — حجزٌ يعود من نسخةٍ ليس حجزًا جديدًا، فلا
  تُجدوَل له رسائل. بدونها كانت الاستعادة سترسل تأكيداتٍ لعميلاتٍ انتهت
  مواعيدهنّ.

ومشغّلات السلامة تعمل كما هي: إسنادُ الرقم عند غيابه، ومزامنةُ البنود،
وإعادةُ حساب المجاميع.

---

## ٨) النشر

الدفع محجوبٌ من بيئة كلاود، فيُبنى حزمةٌ يدفعها صاحب المشروع:

```bash
bash push-lab.sh          # تحديثٌ يمسّ المختبر وحده — يرفض ما يمسّ الحيّ
bash push-to-github.sh    # الترقية، بعد tools/promote.sh
```

`push-lab.sh` يقارن ملفّات الحيّ بين ما على GitHub وما في الحزمة، فإن
تغيّر واحدٌ منها وقف وسمّاه. **لا تتجاوزه لتوفير خطوة.**

من بيئةٍ تملك دفعًا مباشرًا (جهاز صاحب المشروع) يكفي `git push`، ويبقى
`tools/promote.sh --check` قبل أيّ ترقية.

**بعد كلّ نشر:** ارفع `VERSION` في `sw.js`. المستند صار يأتي من الشبكة
أوّلًا فلا يعلق قديمًا، لكنّ رفعَ الرقم يمسح القشرة المحفوظة القديمة عند
التفعيل — وإلّا تراكمت أصولٌ لم يعد لها مرجع.

---

## ٩) أسلوب الكتابة

- الشروح تقول **لماذا** لا **ماذا**. السطر يشرح نفسه؛ اكتب سببه.
- رسالة الكومت: سطر أوّل بصيغة الأمر المؤنّث («اسدّي…»، «ارفعي…»)، ثم
  فقراتٌ تشرح العطب وسببه والحارس الذي مُنع به من العودة.
- الوظيفة الواحدة في موضعٍ واحد. حين تتكرّر قاعدةٌ في مكانين، أخرِجها.
- **لا `console.log` متروك، ولا شيفرة ميتة، ولا `TODO` بلا صاحب.**

---

## ١٠) معلّقٌ اليوم

- ربط واتساب: ينتظر `Phone Number ID` و`Permanent Access Token`.
- أربعة حجوزات مسعّرة خطأً — `EA-20260802` (−٣٠٠) · `EA-20260807` (−٦٠٠)
  · `EA-20260811` (−٢٠٠) · `EA-20260819`. **لم تُمسّ بانتظار قرار صاحب
  المشروع.**
- مؤجَّل: وسوم OG · تنبيه تعارض الحجز اليدوي · قائمة تذكير الغد ·
  استعادة كلمة المرور · سجلّ العميلة · قائمة الانتظار · تغيير الموعد.
